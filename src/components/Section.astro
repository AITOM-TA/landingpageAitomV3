---
interface Props {
  id?: string;
  class?: string;
}
const { id, class: className = '' } = Astro.props;
---

<section
  id={id}
  class={`py-section-sm md:py-section ${className}`}
>
  <slot />
</section>
